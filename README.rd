# golang_mongoDB

To the Run Golang application 
    
    use => " go run main.go "

Add " app.env " file manually and add contents to it like
    
    MONGO_URI ="<Your mongoURI string for the connection>"
    
    SECRET_KEY = "<Your own secreat key>"  secret key your own any string
